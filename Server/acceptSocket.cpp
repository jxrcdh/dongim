#include"acceptSocket.h"
#include<iostream>
#include<sys/socket.h>

using std::cout;
using std::endl;
bool listenFlag = true;
//this function only used to accept socket connect
//than send the connect socket to another thread to process
void acceptSocket()
{
}